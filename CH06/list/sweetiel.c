//sweetiel.c --一个计数循环
#include<stdio.h>
int main()
{
    const int NUMBER=22;
    int count = 1;//初始化取值
    while (count<=NUMBER) //测试
    {
        printf("Be my Valentine !\n");//行为结果
        count++; //更新计数
    }
}
